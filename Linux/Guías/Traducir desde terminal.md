Instala `translate-shell` con el gestor de paquetes de tu sistema.

```shell
sudo dnf install translate-shell
```

Usa el comando `trans` para traducir texto. Por ejemplo:

```shell
trans :es "Hello, how are you?"
```